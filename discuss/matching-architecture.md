# TalentDrop 匹配机制架构设计

> 文档日期：2026-02-24
> 文档定位：核心算法架构设计，指导后续工程实现
> 前置依赖：[brainstorm.md](./brainstorm.md)、[question-bank-full.md](./question-bank-full.md)、[matching-algorithm-research.md](./matching-algorithm-research.md)

---

## 一、设计哲学

### 1.1 我们不是招聘工具，是匹配智能体平台

传统招聘平台的算法目标是 **"让更多人投简历/收简历"**，本质是信息分发。
TalentDrop 的算法目标是 **"90 天后双方满意度最大化"**，本质是关系预测。

这个本质差异决定了整个架构的设计方向：

| 维度 | 传统招聘平台 | TalentDrop |
|------|------------|------------|
| 核心算法 | 关键词匹配 / JD-简历相似度 | 多层深度匹配 + 稳定配对优化 |
| 数据输入 | 简历文本 + JD 文本 | 行为问卷 + 语音 + 开放文本 + 第三方数据 |
| 优化目标 | 点击率 / 投递率 | 90 天留存率 + 双方满意度 |
| 进化方式 | 静态模型定期重训 | 知识图谱增量更新 + 结果反馈持续调优 |
| 可解释性 | "匹配度 85%" | "你们在 X 维度高度互补，因为..." |

### 1.2 从 Date Drop 学到的核心教训

Date Drop 的成功验证了三件事：

1. **深度画像 + 真实结果 = 匹配质量的护城河**。它不是用更好的 UI 赢的，是用更好的数据飞轮赢的
2. **减少选择 = 更强投入**。每周 1 个匹配的约会转化率是 Tinder 的 10 倍
3. **匹配理论（Gale-Shapley）不是学术花瓶**。在 Hinge 和 Date Drop 的实践中，双向稳定匹配将成功率提升了 8 倍

### 1.3 匹配算法的数学目标

```
目标函数：max Σ Satisfaction(candidate_i, job_j, t=90days)

约束条件：
  (1) 稳定性：不存在 (c_i, j_k) 使得双方都更偏好对方而非当前匹配
  (2) 公平性：∀ 受保护群体 g, PassRate(g) >= 0.8 × max(PassRate)
  (3) 容量：每个岗位每周匹配 3-5 人，每个候选人每周匹配 1-3 个岗位
```

---

## 二、三层架构总览

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   Layer 0: 上下文理解层 (Context Intelligence)              │
│   ┌─────────────────────────────────────────────────────┐   │
│   │  LightRAG 知识图谱引擎                               │   │
│   │  · 非结构化文本 → 实体 + 关系                        │   │
│   │  · 双层检索（精确实体 / 宏观关系）                    │   │
│   │  · 增量更新，持续丰富                                │   │
│   └─────────────────────────┬───────────────────────────┘   │
│                             │ 结构化上下文                   │
│                             ▼                               │
│   Layer 1-5: 匹配算法层 (Matching Engine)                   │
│   ┌─────────────────────────────────────────────────────┐   │
│   │  L1 硬性过滤 → L2 DNA 兼容性 → L3 技能匹配          │   │
│   │  → L4 文化契合 → L5 稳定配对优化                     │   │
│   └─────────────────────────┬───────────────────────────┘   │
│                             │ 匹配结果 + 反馈信号            │
│                             ▼                               │
│   Layer 6-7: 进化层 (Evolution Engine)                      │
│   ┌─────────────────────────────────────────────────────┐   │
│   │  L6 协同过滤增强 · L7 公平性约束                     │   │
│   │  · 结果反馈 → 权重调优 → 题库迭代                    │   │
│   └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、Layer 0: 上下文理解层（LightRAG 知识图谱引擎）

### 3.1 为什么需要这一层

Career DNA 问卷能产出 8 个核心维度的**结构化评分**，但 TalentDrop 还有大量**非结构化数据**无法被简单量化：

- 候选人的 Dream Role 开放式描述
- AI 语音对话的转录文本
- 企业员工匿名评价
- Glassdoor / Blind 等第三方评价
- 推荐人的 "为什么 TA 适合这个团队" 文字

这些非结构化文本蕴含着问卷无法捕捉的深层信号。LightRAG 的作用是将它们转化为可检索、可推理的结构化知识。

### 3.2 知识图谱设计

**实体类型：**

| 实体类型 | 示例 | 来源 |
|----------|------|------|
| Person | 候选人、员工、推荐人 | 注册信息 |
| Skill | React, 财务建模, 团队管理 | 问卷 + 简历 + 语音 |
| Value | 自主性、使命驱动、工作生活平衡 | 问卷 + 开放文本 |
| Culture | 扁平管理、快速迭代、深度打磨 | 企业问卷 + 员工评价 |
| Team | 某公司某部门某团队 | 企业注册 |
| Role | 全栈工程师、产品经理 | 岗位发布 |
| Outcome | 入职、留存、离职、高绩效 | 90 天追踪 |

**关系类型：**

| 关系 | 示例 | 价值 |
|------|------|------|
| values → | Person -[values]→ Value | 候选人重视什么 |
| embodies → | Team -[embodies]→ Culture | 团队真实文化 |
| requires → | Role -[requires]→ Skill | 岗位需要什么技能 |
| complements → | Value -[complements]→ Culture | 价值观与文化互补 |
| conflicts_with → | Value -[conflicts_with]→ Culture | 潜在文化冲突 |
| led_to → | Match -[led_to]→ Outcome | 匹配导致了什么结果 |
| similar_to → | Person -[similar_to]→ Person | 画像相似的候选人 |

### 3.3 LightRAG 双层检索的应用

**低层检索（Low-Level）— 精确实体查询：**

```
查询："候选人 A 具体擅长什么技术？"
→ 命中实体：Person_A → [masters] → {React, TypeScript, GraphQL}
→ 一跳扩展：React → [related_to] → {Next.js, Redux}
→ 输出：候选人 A 精通 React 生态（React, Next.js, Redux）+ TypeScript + GraphQL
```

**高层检索（High-Level）— 跨实体宏观推理：**

```
查询："什么样的人在快速迭代文化中表现最好？"
→ 命中关系：Culture_快速迭代 ← [embodies] ← {Team_X, Team_Y, Team_Z}
→ 关联扩展：这些团队中 Outcome_高绩效 的 Person 有什么共同 Value？
→ 输出：在快速迭代文化中，"拥抱模糊" + "直接坦率" 维度得分高的候选人
         留存率比平均高 40%
```

### 3.4 增量更新策略

```
事件触发                    更新动作
─────────────────────────────────────────────────
新候选人完成问卷       →    新增 Person 节点 + 关系边
新企业发布岗位         →    新增 Role 节点 + requires 关系
候选人 AI 语音对话     →    提取实体/关系，并入已有图谱
员工提交匿名评价       →    更新 Team 的 embodies 关系
90 天追踪结果回传      →    新增 Outcome 节点 + led_to 关系
Glassdoor 新评价爬取   →    增量合并到企业文化子图
```

关键优势：无需重建整个图谱，每次增量操作只处理新数据，成本可控。

---

## 四、Layer 1-5: 匹配算法层

### 4.1 L1: 硬性过滤（Dealbreakers）

在进入计算密集的匹配之前，先用硬性条件快速缩小候选池。

**候选人侧 Dealbreakers：**
- 工作地点（城市 / 远程 / 混合）
- 最低薪资期望
- 签证/工作许可
- 公司规模偏好（硬性）

**企业侧 Dealbreakers：**
- 经验年限要求（最低）
- 必须具备的技能（硬性）
- 工作授权要求

**实现方式：** 布尔过滤，O(n) 线性扫描，不涉及任何 ML 计算。

> 借鉴来源：Hinge Dealbreaker 机制——硬性条件不参与评分，直接排除

### 4.2 L2: Career DNA 兼容性评分

这是匹配的核心层，基于 8 维 Career DNA 光谱评分。

**数据来源：**
- **C_d（候选人侧）**：Career DNA 30 题标准题库 → 详见 [question-bank-full.md](./question-bank-full.md)
- **J_d（企业侧）**：Company DNA 20 题 × N 人聚合（去极值加权中位数，HR 权重 0.5，员工 1.0）→ 详见 [company-dna-design.md](./company-dna-design.md) 第六节

**评分公式（借鉴 OkCupid 加权几何平均）：**

```
对于候选人 C 和岗位 J（代表企业+团队+角色的综合画像）：

Step 1: 单维度匹配分
  对于每个维度 d ∈ {Pace, Collab, Decision, Expression, Unc, Growth, Motiv, Execution}:
    score_d = 1 - |C_d - J_d| / scale_d
    其中 C_d 和 J_d 是各自在该维度光谱上的位置（0-100）
    scale_d 是该维度的归一化因子

Step 2: 一致性加权
  每个维度的匹配分乘以该候选人在该维度的一致性系数：
    weighted_score_d = score_d × consistency_d
  consistency_d 来自交叉验证检测（同一维度被 4-5 道题覆盖）
  一致性低 → 该维度数据不可信 → 降低权重

Step 3: 维度重要性加权
  不同岗位对不同维度的权重不同（由 L0 知识图谱推断 + 企业设定）：
    Score_C→J = Σ(importance_d × weighted_score_d) / Σ(importance_d)

Step 4: 双向几何平均
  Match% = √(Score_C→J × Score_J→C)

  为什么用几何平均？
  · 算术平均：(0% + 100%) / 2 = 50%  ← 一方完全不满意仍得 50%
  · 几何平均：√(0% × 100%) = 0%      ← 正确反映"匹配必须双向"
```

**维度权重的来源（优先级从高到低）：**

1. 结果数据学习：该维度在历史匹配中对 90 天满意度的预测力（数据充足后）
2. 岗位类型默认权重：工程师岗的 "Decision" 权重 > 设计师岗（平台预设）
3. 企业显式设定：企业可在 Company DNA 中强调特定维度

### 4.3 L3: 技能语义匹配

问卷测量的是"人格-文化契合"，但技能匹配同样重要。

**技术路线（借鉴 Eightfold AI）：**

```
Step 1: 技能嵌入
  使用预训练语言模型将技能短语映射到向量空间：
    embed("React") → [0.23, -0.41, ..., 0.87]  (N维)
    embed("Vue.js") → [0.21, -0.39, ..., 0.85]  (N维)
    cosine_similarity(React, Vue.js) ≈ 0.92  ← 语义相近

Step 2: 候选人技能向量
  将候选人的所有技能嵌入取加权平均：
    V_candidate = Σ(freshness_i × proficiency_i × embed(skill_i)) / n
  freshness_i: 技能新鲜度（最近使用的权重更高）
  proficiency_i: 熟练程度（问卷/简历推断）

Step 3: 岗位需求向量
  V_job = Σ(importance_i × embed(required_skill_i)) / m

Step 4: 技能匹配分
  SkillMatch = cosine_similarity(V_candidate, V_job)
```

### 4.4 L4: 文化契合度

超越简单的"你们的值一样"，使用学术标准方法量化人-组织契合。

**E_k 数据来源：** Company DNA 20 题经 HR + 员工聚合后的维度评分。聚合算法（去极值加权中位数）和真实度验证（Culture Authenticity Score）详见 [company-dna-design.md](./company-dna-design.md)。当 CAS 低于阈值时，E_k 的置信度系数会降低该维度在匹配中的权重。

**技术路线（借鉴 Edwards P-E Fit 多项式回归）：**

```
对于每个文化维度 k：
  Z_k = b₀ + b₁P_k + b₂E_k + b₃(P_k × E_k) + b₄P_k² + b₅E_k²

其中：
  P_k = 候选人在维度 k 上的值（Career DNA）
  E_k = 企业在维度 k 上的值（Company DNA，由员工匿名填写聚合）
  Z_k = 该维度的契合度预测值（指向 90 天满意度）
  b₀-b₅ = 从历史数据中学习的回归系数

交互项 P_k × E_k 的含义：
  · 正系数 → 补充型契合（双方都高时效果最好）
  · 负系数 → 互补型契合（一方高一方低时效果最好）

二次项 P_k², E_k² 的含义：
  · 负系数 → 存在最优点（倒U形关系，过高过低都不好）
  · 正系数 → 极端值反而更好
```

**冷启动时的降级策略：**
回归系数需要足够多的结果数据才能学习。冷启动阶段降级为简化的距离度量：

```
CultureFit_k = 1 - |P_k - E_k| / max_range
CultureFit_total = mean(CultureFit_k for all k)
```

### 4.5 L5: 稳定匹配优化（Gale-Shapley）

L2-L4 为每对 (候选人, 岗位) 计算出一个综合偏好分。L5 用这些分数进行全局最优的配对。

**为什么需要这一层？**

L2-L4 只是逐对评分。但在 Weekly Drop 中，一个岗位只能匹配 3-5 人，一个候选人只能收到 1-3 个匹配。如果只按分数贪心分配，可能产生"不稳定匹配"——即存在某对双方都更想要对方但没被配到一起。

**Gale-Shapley 延迟接受算法：**

```
输入：
  · 候选人集合 C = {c₁, c₂, ..., cₙ}
  · 岗位集合 J = {j₁, j₂, ..., jₘ}
  · 偏好矩阵 Pref_C[i][j] = 候选人 i 对岗位 j 的综合评分
  · 偏好矩阵 Pref_J[j][i] = 岗位 j 对候选人 i 的综合评分
  · 容量 cap_j = 岗位 j 本周可匹配的人数上限

算法：
  while 存在未匹配且仍有可提议对象的候选人:
    每个未匹配候选人 c_i → 向其偏好列表中最优先的（未被拒绝的）岗位提议
    每个岗位 j_k → 从所有提议者 + 当前暂定匹配中，选择 top cap_j 个
                   对其余所有人回复"拒绝"

输出：稳定匹配（不存在双方都更偏好对方的阻塞对）
时间复杂度：O(n × m)
```

**TalentDrop 的改造：**

| 经典 Gale-Shapley | TalentDrop 改造 |
|-------------------|----------------|
| 需要完整偏好排序 | 用 L2-L4 综合分替代显式排序 |
| 二元分组（男/女） | 无分组——候选人 vs 岗位 |
| 一对一匹配 | 一对多：一个岗位可配多人 |
| 一次性运行 | 每周运行一次，pool 动态变化 |

**综合评分的计算：**

```
TotalScore(c_i, j_k) = w₁ × DNAMatch(c_i, j_k)      // L2 Career DNA 兼容性
                     + w₂ × SkillMatch(c_i, j_k)      // L3 技能匹配
                     + w₃ × CultureFit(c_i, j_k)      // L4 文化契合
                     + w₄ × TeamChemistry(c_i, j_k)   // 团队互补度（L0 提供）

初始权重（冷启动）：w₁=0.40, w₂=0.25, w₃=0.25, w₄=0.10
权重随数据积累由 L6 进化层动态调优
```

---

## 五、Layer 6-7: 进化层

### 5.1 L6: 协同过滤增强

**为什么不从一开始就用协同过滤？**

人才匹配领域的协同过滤面临严重的冷启动和稀疏性问题——一个人入职后很久才换工作，交互数据极少。因此 TalentDrop 的策略是：

```
阶段 1（冷启动）：100% 基于内容（L1-L5）
阶段 2（数据积累）：90% 内容 + 10% 协同过滤
阶段 3（成熟期）：70% 内容 + 30% 协同过滤
```

**协同过滤的具体实现（借鉴 Coffee Meets Bagel）：**

```
Step 1: 构建交互矩阵
  R[i][j] = 候选人 i 对岗位 j 的隐式评分
  评分来源：
    Accept = +3
    查看报告 > 60s = +1
    Pass = -1
    面试 = +5
    入职 = +10
    90天留存 = +15

Step 2: 矩阵分解（隐因子模型）
  R ≈ P × Q^T
  P: 候选人矩阵 (n × k)，每个候选人 k 个隐因子
  Q: 岗位矩阵 (m × k)，每个岗位 k 个隐因子
  k = 50-100（隐因子数量）

Step 3: 预测未知匹配的偏好
  R̂[i][j] = P[i] · Q[j]^T

Step 4: 融入内容评分
  FinalScore = α × ContentScore(L2-L5) + (1-α) × CFScore(L6)
  α 从 1.0 逐步降低到 0.7（随数据量增加）
```

**协同过滤能发现什么？**

内容匹配无法捕捉的隐式模式。例如：
- "在问卷上表现为独立驱动的候选人，实际上在团队协作强的公司留存率更高"
- "同一所学校毕业、经历相似的人，对同一类公司的满意度具有强相关"

### 5.2 L7: 公平性约束

**偏见检测框架（借鉴 Pymetrics audit-ai）：**

```
对于每个受保护群体 g ∈ {性别, 种族, 年龄段, 学历背景, ...}:

  检测 1: 四分之五规则 (4/5 Rule)
    MatchRate(g) / max(MatchRate) >= 0.80
    如果违反 → 标记为"显著差异"

  检测 2: 统计显著性检验
    对 MatchRate(g) vs MatchRate(overall) 进行 z-test
    如果 p < 0.05 → 标记为"统计显著偏见"

  缓解措施:
    · 识别导致差异的特征维度
    · 从模型中移除或降权该特征
    · 重新运行匹配，验证偏见消除
```

**关键原则：**
- 候选人的人口统计信息**不参与**匹配算法的任何层
- 人口统计信息**仅用于**事后的偏见审计
- 审计频率：每月一次全量审计 + 每周 Drop 后的快速检查

### 5.3 持续进化的反馈闭环

```
                                    ┌─────────────┐
                                    │  题库迭代    │
                                    │ 低效题替换   │
                                    │ 新题 A/B test│
                                    └──────┬──────┘
                                           │
┌──────────┐    ┌──────────────┐    ┌──────┴──────┐    ┌──────────┐
│ 问卷回答  │───→│  匹配算法    │───→│  Weekly Drop│───→│ 用户反馈  │
│ 语音对话  │    │  L1-L5       │    │  推送匹配   │    │ Accept/  │
│ 开放文本  │    └──────────────┘    └─────────────┘    │ Pass     │
└──────────┘           ↑                                └────┬─────┘
                       │                                     │
                ┌──────┴──────┐                              │
                │ 权重调优     │                              │
                │ w₁,w₂,w₃,w₄│                              │
                │ 动态更新     │                              │
                └──────┬──────┘                              │
                       │                                     │
                ┌──────┴──────────────────────────────┐      │
                │         结果数据                     │←─────┘
                │  面试结果 / 入职 / 90天留存 / 满意度  │
                └──────┬──────────────────────────────┘
                       │
              ┌────────┴────────┐
              │                 │
        ┌─────┴─────┐   ┌──────┴──────┐
        │ LightRAG  │   │ 协同过滤     │
        │ 知识图谱   │   │ 隐因子更新   │
        │ 增量更新   │   │             │
        └───────────┘   └─────────────┘
```

**权重调优的具体方法：**

```
每积累 N 个完整结果（面试→入职→90天），执行一次权重优化：

objective: max correlation(TotalScore, Satisfaction_90day)

method: 贝叶斯优化 (Bayesian Optimization)
  · 搜索空间: w₁ ∈ [0.2, 0.6], w₂ ∈ [0.1, 0.4], w₃ ∈ [0.1, 0.4], w₄ ∈ [0.0, 0.2]
  · 约束: w₁ + w₂ + w₃ + w₄ = 1
  · 评估指标: Spearman rank correlation with 90-day satisfaction

触发条件: 每积累 100 个完整结果 或 每季度（取先到者）
```

**题库迭代的具体方法：**

```
对于每道题目 q:
  · 区分度 = std(responses_q)
    如果 > 60% 选同一选项 → 标记为"区分度不足"
  · 预测力 = correlation(response_q, satisfaction_90day)
    如果 < 0.1 → 标记为"预测力弱"
  · 抗博弈性 = variance(match_rate | response_option)
    如果某选项的匹配成功率显著高于其他 → 标记为"可博弈"

触发替换: 任何一项指标不达标 → 进入候选替换池
替换流程: 新题 A/B test 2 周 → 效果对比 → 确认替换或回滚
```

---

## 六、匹配报告生成

每次 Weekly Drop 不只推送一个匹配，还附带一份"为什么你们适合"的报告。这是 LightRAG 上下文理解层的**直接产出**。

### 6.1 报告结构

```
┌─────────────────────────────────────────────┐
│  🎯 Match Report: [候选人] × [岗位@公司]     │
├─────────────────────────────────────────────┤
│                                             │
│  Overall Match: 87%                         │
│  ████████████████████░░░                    │
│                                             │
│  ── Why You're a Great Fit ──               │
│                                             │
│  1. Work Style Alignment (92%)              │
│     You both thrive in fast-paced           │
│     environments with high autonomy.        │
│                                             │
│  2. Growth Path Match (89%)                 │
│     This role offers the technical depth    │
│     you're looking for, with a clear path   │
│     to Staff Engineer.                      │
│                                             │
│  3. Culture Compatibility (85%)             │
│     The team values direct communication    │
│     and data-driven decisions — matching    │
│     your preferred style.                   │
│                                             │
│  ── Something to Explore ──                 │
│                                             │
│  Your preference for deep work may need     │
│  balancing with the team's collaborative    │
│  sprint culture. Worth discussing!          │
│                                             │
└─────────────────────────────────────────────┘
```

### 6.2 报告生成流程

```
Step 1: 从 L2-L5 获取维度匹配分
Step 2: 从 LightRAG 高层检索获取宏观洞察
  查询: "什么特征的人在 [这个团队的文化] 中表现最好？"
Step 3: 从 LightRAG 低层检索获取具体细节
  查询: "[候选人] 的核心价值观和工作风格"
  查询: "[团队] 的真实文化特征"
Step 4: 用 LLM 将数据 + 洞察组装成可读报告
Step 5: 高亮互补点 + 标记潜在张力点
```

---

## 七、三阶段进化路径

### Phase 1: 冷启动期（0 - 1,000 用户）

| 维度 | 策略 |
|------|------|
| 数据来源 | 问卷结构化数据为主，少量开放文本 |
| 匹配算法 | L1(硬性过滤) + L2(DNA兼容性简化版) + L3(技能嵌入) |
| L4 文化契合 | 降级为距离度量（无回归系数） |
| L5 稳定匹配 | 候选池小时可简化为贪心匹配 |
| LightRAG | 处理开放文本，生成匹配报告 |
| 权重 | 使用专家预设的初始权重 |
| 关键指标 | 双方 Accept Rate > 50% |

### Phase 2: 数据积累期（1,000 - 10,000 用户）

| 维度 | 策略 |
|------|------|
| 数据来源 | 问卷 + 开放文本 + 语音 + 第一批结果反馈 |
| 匹配算法 | 完整 L1-L5 |
| L4 文化契合 | 开始训练 Edwards 多项式回归（需足够结果数据） |
| L5 稳定匹配 | 正式运行 Gale-Shapley |
| LightRAG | 增量更新知识图谱，开始发现文化模式 |
| 权重 | 首次基于结果数据的权重调优 |
| 关键指标 | 面试转化率 > 40%，90 天留存率开始追踪 |

### Phase 3: 智能进化期（10,000+ 用户）

| 维度 | 策略 |
|------|------|
| 数据来源 | 全量数据 + 充足的 90 天结果反馈 |
| 匹配算法 | L1-L5 + L6(协同过滤) + L7(偏见审计) |
| L6 协同过滤 | 隐因子模型正式上线，融入内容评分 |
| LightRAG | 知识图谱成熟，多跳推理发现隐式成功因素 |
| 权重 | 贝叶斯优化持续调优，题库定期迭代 |
| 关键指标 | 90 天留存率 > 85%，NPS > 60 |

---

## 八、技术栈初步规划

| 组件 | 技术选型 | 理由 |
|------|---------|------|
| **LightRAG 图存储** | Neo4j 或 PostgreSQL AGE | 知识图谱查询、多跳推理 |
| **LightRAG 向量存储** | Qdrant 或 Milvus | 嵌入检索，支持增量更新 |
| **LightRAG KV 存储** | PostgreSQL | 与主数据库统一 |
| **嵌入模型** | text-embedding-3-large 或 bge-m3 | 技能/文本语义嵌入 |
| **LLM（提取+生成）** | Claude API | 实体关系提取、报告生成 |
| **匹配算法引擎** | Python (NumPy + SciPy) | Gale-Shapley、矩阵运算 |
| **协同过滤** | Implicit / Surprise | 隐因子矩阵分解 |
| **调度** | Celery + Redis | 每周 Drop 的定时任务 |
| **前端** | Next.js 15+ / React 19+ / Tailwind v4 | 遵循项目规范 |
| **主数据库** | PostgreSQL | 结构化数据存储 |

---

## 九、与赛道 2 叙事的对齐

回到 "与智能体共生与智能市场" 的赛道定位：

| 赛道 2 关键问题 | TalentDrop 的回答 |
|----------------|-------------------|
| 智能体如何获得长期、海量且有效的上下文？ | LightRAG 知识图谱 = 匹配智能体的长期记忆，增量更新 = 持续学习 |
| 最佳切入点是什么？ | Career DNA + Company DNA = 深度结构化的上下文获取协议 |
| 如何设计智能体工作流，而不仅是对话？ | 问卷采集 → 知识图谱构建 → 多层匹配 → Weekly Drop → 结果反馈 → 进化 |
| 数据、感知、执行与激励如何协同？ | 数据(问卷+语音+第三方) → 感知(LightRAG理解) → 执行(Gale-Shapley匹配) → 激励(90天信誉分) |

**一句话定位：**

> TalentDrop 是一个让匹配智能体拥有"深度人格理解能力"的上下文获取与工作流平台。
> 通过 LightRAG 知识图谱持续构建对人和组织的深度理解，
> 通过 Gale-Shapley 稳定匹配算法实现双向最优配对，
> 通过真实结果反馈驱动智能体的持续进化。

---

## 附录 A: 核心参考来源

### 匹配理论
- Gale & Shapley (1962). "College Admissions and the Stability of Marriage." *American Mathematical Monthly*
- Roth (2002). Nobel Prize 相关工作，Stable Matching 在医学驻院匹配中的应用

### 人-组织契合
- Edwards & Parry (1993). 多项式回归替代差值法. *Academy of Management Journal*
- Kristof (1996). P-O Fit 整合综述. *Personnel Psychology*

### 行业实践
- Date Drop: 深度画像 + 真实结果训练 + Gale-Shapley 匹配
- Hinge: ML 偏好推断 + Gale-Shapley "Most Compatible"（成功率 8x）
- OkCupid: 加权问答 + 几何平均数（公式最透明的匹配系统）
- Coffee Meets Bagel: 深度神经网络 + 100 维隐因子协同过滤
- Eightfold AI: 16 亿画像训练的技能嵌入 + 余弦相似度
- Pymetrics: 神经科学游戏 + 集成学习 + audit-ai 偏见检测

### 技术框架
- LightRAG (HKUDS, EMNLP 2025): 知识图谱增强 RAG，双层检索 + 增量更新
